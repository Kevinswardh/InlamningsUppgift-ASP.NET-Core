@using _1.PresentationLayer.ViewModels.MembersViewModels
@model UserListViewModel

@if (Model.TotalPages > 1)
{
    <nav class="pagination-wrapper">
        <ul class="pagination">
            @for (int i = 1; i <= Model.TotalPages; i++)
            {
                <li>
                    <a class="@(i == Model.CurrentPage ? "pagination-button active-page" : "pagination-button")"
                       href="@Url.Action("Index", Model.TabData.CurrentController, new { page = i, tab = Model.TabData.SelectedTab, sortBy = Model.SelectedSort })">
                        @i
                    </a>
                </li>
            }
        </ul>
    </nav>
}
